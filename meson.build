project('amtk', 'c',
  version : '5.1.0',
  meson_version : '>=0.52',
  default_options : [
    'buildtype=debugoptimized',
    'warning_level=1',
  ]
)

AMTK_API_VERSION = '5'

GNOME = import('gnome')
pkg = import('pkgconfig')

subdir('amtk')
