project('amtk', 'c',
  version : '5.0.1',
  meson_version : '>=0.52',
  default_options : [
    'buildtype=debugoptimized',
    'warning_level=1',
  ]
)

gnome = import('gnome')
pkg = import('pkgconfig')

subdir('amtk')
